A rules file is the governance contract; enforcing test directory structure, naming conventions, gating, environment handling, and lifecycle completeness clarifies agent behavior and prevents vague compliance.
- It enables automation (CI bots) to parse expectations (e.g. coverage target, gating variable names).
- Explicit negative testing requirements ensure API edge cases are not neglected.
